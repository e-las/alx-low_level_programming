#include "main.h"
/**
 * _isdigit - A program that checks for digit 0 to 9
 * @c: the number to be check
 * Return: 1 if c is a number and 0 otherwise
 */
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
