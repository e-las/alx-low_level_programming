#include <stdio.h>
#include <stdlib.h>
/**
 * main - this is where your program execution start
 * return: each time 0
 */
int main()
{
        int u;
  
        for (u = 0; u <= 10; u++)

        printf("%d", u);

        printf("\n");
        return (0);
}
