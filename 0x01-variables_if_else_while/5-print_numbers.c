#include <stdio.h>
#include <stdlib.h>
/**
 * main - this is where your program execution start
 * Return: each time 0
 */
int main(void)
{
	int u;

	for (u = 0; u <= 9; u++)

	printf("%d", u);

	printf("\n");
	return (0);
}
