#include <stdio.h>
/**
 * main - this is the execution of the program start
 * Return: each time 0
 */
int main(void)
{
	char u;

	u = 'a';

	while
		(u <= 'z')
		{
			putchar(u);
			u++;
		}
	putchar('\n');
	return (0);
}
