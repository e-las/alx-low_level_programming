#include <stdio.h>
/**
 * main - this is the execution of the program start
 * return: each time 0
 */
int main()
{
	char u;
	u = 'a';
	while
		(u <= 'z')
		{
			putchar(u);
			u++;
		}
	putchar('\n');
	return (0);
}
